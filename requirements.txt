# Core numerical / signal-processing dependencies (used by the notebook
# and by the embedded production core extracted from the source scripts).
numpy>=1.21
scipy>=1.7
PyWavelets>=1.3
matplotlib>=3.6

# Notebook-only helpers.
pandas>=1.3
tqdm>=4.62

# Imported by the embedded production core (dif_time_g1.py and
# diff_velocity_g1.py). Not listed in the notebook's own package note,
# but required for `exec` to succeed.
psutil>=5.9
